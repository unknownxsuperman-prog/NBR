# Add project-specific ProGuard rules here.
# By default, no obfuscation rules are needed since minifyEnabled is false
# in the release build type above. Flip it on and add WebView / Compose
# keep rules here if you later enable R8 minification.
