package com.unknownxsuperman.glass.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Palette mirrors the CSS custom properties used in the desktop app's
 * new-tab page 1:1 — keep both in sync if you retheme.
 */
object AppColors {
    val Bg = Color(0xFF000000)
    val Surface1 = Color(0xFF1C1C1E)
    val Surface2 = Color(0xFF232325)
    val Surface3 = Color(0xFF2C2C2E)
    val Surface4 = Color(0xFF3A3A3C)
    val Border = Color(0x12FFFFFF)        // ~ rgba(255,255,255,0.07)
    val BorderStrong = Color(0x24FFFFFF)  // ~ rgba(255,255,255,0.14)
    val TextPrimary = Color(0xFFFFFFFF)
    val TextSecondary = Color(0xFFA6A6AB)
    val TextTertiary = Color(0xFF6E6E73)
    val Accent = Color(0xFF0A84FF)
    val Red = Color(0xFFFF453A)
    val BrandRed = Color(0xFFD71921)      // the logo's actual brand red
}
